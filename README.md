# redis-dotnet

A [redis:latest](https://hub.docker.com/_/redis) based container image that includes the [dotnet 10.0 sdk](https://dotnet.microsoft.com/en-us/download/dotnet/10.0).

It's very useful for running integration tests for dotnet applications that have a dependency on Redis.
